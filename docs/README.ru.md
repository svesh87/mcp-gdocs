*[English](../README.md) · Русский*

# mcp-gdocs

MCP-сервер для Google Slides, Sheets, Docs и Drive, который работает от имени одного
вошедшего человека и правит презентации так, как это делает сам редактор.

Ради презентаций всё и затевалось. Универсальный гугловый сервер правит колоду тем, что
двигает текстовые блоки по выдуманным координатам, пишет символы маркеров в обычный текст
и вместо настоящей таблицы кладёт россыпь фигур. Получается то, что выглядит сломанным у
всех, кто это открыл, и чинится только переделкой слайда. Здешние инструменты работают
вместе с шаблоном, а не в обход него: родные вложенные списки, стили, снятые с собственных
слайдов колоды, настоящие таблицы и миниатюра, чтобы проверить результат.

## Почему узкие инструменты по слайдам работают

Каждый пункт — решение, а не деталь реализации:

- **Родные списки, а не нарисованные руками.** `gdocs_slides_replace_body_nested_list`
  отправляет текст с табуляциями для глубины и просит Slides сделать из него список.
  Дальше Google сам считает отступы, маркеры и интервалы по шаблону. Сервер, который
  расставляет маркеры сам, попадает почти точно, а это хуже, чем мимо.
- **Стиль копируется, а не выдумывается.** `gdocs_slides_copy_title_style` читает первый
  ран настоящего заголовка и переносит эти поля. Никто не гадает, какой тут «шрифт
  заголовков».
- **Сначала посмотреть.** `gdocs_slides_inspect_text_structure` показывает, что лежит в
  текстовом блоке — абзацы, маркеры, вложенность, — до того как это заменят.
- **Таблица должна быть таблицей.** `gdocs_slides_create_table_with_text` создаёт
  настоящую таблицу слайдов с ширинами колонок, шрифтами и выравниванием по колонкам.
  Ряды текстовых блоков похожи на таблицу ровно до первой правки.
- **Проверять глазами.** `gdocs_slides_export_thumbnail` отрисовывает слайд, чтобы агент
  посмотрел на сделанное, а не поверил отправленному батчу.
- **Никаких произвольных батчей.** Инструмента, который передаёт в
  `presentations.batchUpdate` запросы вызывающего, здесь нет. Именно он и делает колоды
  кривыми.

Подход взят с рабочего сервера, который коллега написал ровно под эту задачу; здесь то же
поведение на Go и остальные гугловые редакторы вокруг.

## Удаления нет нигде

Нет инструмента, который удаляет файл, слайд, лист или строку, — не выключен, не за
флагом: такого кода нет. Агент, «прибравшийся» в общем диске, — инцидент, который никто не
отыграет назад, и самый дешёвый способ его не иметь — не иметь того, что его вызывает.
Тест проверяет это по именам зарегистрированных инструментов.

Всё, что вообще что-то меняет, требует `--allow-write`. Без него сервер отдаёт только
читающие инструменты.

## Инструменты

Их пятьдесят два: пятнадцать читают, тридцать семь что-то меняют. Сервер, поднятый без
`--allow-write`, регистрирует пятнадцать и больше ничего.

Чтение, всегда:

| Инструмент | Что делает |
|---|---|
| `gdocs_slides_list` | слайды колоды с объектами на каждом: идентификаторы, виды, плейсхолдеры, размеры |
| `gdocs_slides_inspect_page` | один слайд целиком: фон, заметки и каждый элемент с боксом, поворотом, порядком наложения, заливкой, контуром и обрезкой |
| `gdocs_slides_read_theme` | палитра, фон мастера и каждый макет со стилями, которые навязывают его плейсхолдеры |
| `gdocs_slides_list_layouts` | макеты, которые предлагает собственный шаблон колоды |
| `gdocs_slides_inspect_text_structure` | абзацы текстового блока: маркеры, вложенность, стили, интервалы и отступы |
| `gdocs_slides_inspect_title_style` | как строка текста выглядит на самом деле — слияние текста, макета и мастера |
| `gdocs_slides_read_table` | ячейки таблицы, ширины колонок, высоты строк и оформление ячеек |
| `gdocs_slides_export_thumbnail` | отрисованная картинка одного слайда |
| `gdocs_sheets_info` | листы таблицы, их идентификаторы и размеры |
| `gdocs_sheets_read` | ячейки диапазона или целого листа |
| `gdocs_sheets_read_format` | диапазон по ячейкам: ширины, высоты, объединения, ссылки, заметки, выпадающие списки |
| `gdocs_sheets_read_dropdown_colors` | цвета вариантов выпадающего списка, которых нет ни в одном ответе API |
| `gdocs_docs_read` | документ как текст, таблицы — через табуляцию |
| `gdocs_docs_read_structure` | документ как он устроен: абзацы, прогоны, списки, таблицы, разделы, колонтитулы, именованные стили, картинки |
| `gdocs_drive_search` | файлы на диске, включая общие диски |
| `gdocs_drive_file_info` | имя, вид, владельцы и папки одного файла |
| `gdocs_drive_export` | файл, выгруженный в другой формат |

Запись, только с `--allow-write`.

Слайды, содержимое колоды:

| Инструмент | Что делает |
|---|---|
| `gdocs_slides_copy_presentation` | завести колоду из шаблона, сохранив мастер, макеты и шрифты |
| `gdocs_slides_add_slide` | добавить слайд по одному из собственных макетов колоды |
| `gdocs_slides_set_text` | заменить текст блока, оформление оставив шаблону |
| `gdocs_slides_replace_body_nested_list` | пересобрать слайд с тезисами как родной вложенный список |
| `gdocs_slides_create_table_with_text` | настоящая таблица с ширинами, шрифтами, цветами и выравниванием |
| `gdocs_slides_update_table_cells` | новые значения в существующей таблице, с сохранением ширин и оформления |
| `gdocs_slides_style_table` | объединить ячейки, залить, выровнять содержимое, задать высоты строк |
| `gdocs_slides_insert_image` | поставить картинку на слайд по адресу |
| `gdocs_slides_create_shape` | текстовый блок либо плашка, стрелка или круг — с текстом, заливкой и контуром |
| `gdocs_slides_create_line` | линия, стрелка или коннектор |
| `gdocs_slides_set_speaker_notes` | заменить заметки за слайдом |
| `gdocs_slides_reorder` | выставить слайды в нужном порядке |
| `gdocs_slides_delete` | убрать слайд или элемент слайда — и ничего за пределами колоды |

Слайды, как это выглядит:

| Инструмент | Что делает |
|---|---|
| `gdocs_slides_place_element` | подвинуть, изменить размер, повернуть или отразить — по краю, по соседу или по месту образца |
| `gdocs_slides_order_elements` | что поверх чего |
| `gdocs_slides_group` | собрать элементы в группу или разобрать группы |
| `gdocs_slides_set_page_background` | цвет, картинка или обратно к макету |
| `gdocs_slides_style_shape` | заливка, контур и выравнивание содержимого фигуры |
| `gdocs_slides_style_image` | обрезка, прозрачность, яркость, контраст и рамка картинки |
| `gdocs_slides_set_text_style` | кегль, шрифт, насыщенность, курсив, цвет — значением или именем темы |
| `gdocs_slides_set_paragraph_style` | выравнивание, межстрочный интервал, отступы вокруг абзацев и слева |
| `gdocs_slides_copy_title_style` | перенести стиль настоящего заголовка на другой, хоть из другой колоды |
| `gdocs_slides_reset_text_style` | вернуть текст его макету |
| `gdocs_slides_link_text` | сделать кусок текста ссылкой |
| `gdocs_slides_style_layout` | записать стиль в макет или мастер, чтобы его наследовал каждый слайд на нём |
| `gdocs_slides_set_theme_colors` | заменить палитру колоды — все двенадцать цветов разом |

Таблицы, документы и диск:

| Инструмент | Что делает |
|---|---|
| `gdocs_sheets_write` | записать прямоугольник ячеек |
| `gdocs_sheets_append` | дописать строки после последней заполненной, вставкой, а не поверх |
| `gdocs_sheets_create` | создать таблицу с локалью и размером каждого листа |
| `gdocs_sheets_add_tab` | добавить лист нужного размера |
| `gdocs_sheets_duplicate_tab` | скопировать лист внутри той же книги |
| `gdocs_sheets_update_properties` | название, локаль и часовой пояс существующей книги |
| `gdocs_sheets_format_cells` | шрифт, начертание, цвета, оба выравнивания, перенос, формат числа, поворот, поля, ссылка, заметка |
| `gdocs_sheets_set_text_runs` | разное оформление внутри одной ячейки |
| `gdocs_sheets_set_borders` | границы прямоугольника и линии внутри него |
| `gdocs_sheets_set_validation` | повесить выпадающий список на прямоугольник ячеек |
| `gdocs_sheets_set_conditional_format` | красить ячейки по содержимому: условие или градиент |
| `gdocs_sheets_set_banding` | чередование полос, которое следует за добавленными строками |
| `gdocs_sheets_set_filter` | фильтр листа: что прячет каждая колонка и как сортирует |
| `gdocs_sheets_protect_range` | закрыть диапазон от правок |
| `gdocs_sheets_add_named_range` | дать диапазону имя для формул |
| `gdocs_sheets_set_layout` | ширины, высоты, закрепления, объединения, скрытие, цвет ярлыка, рост сетки |
| `gdocs_sheets_insert_dimensions` | вставить строки или колонки в середину или в конец |
| `gdocs_sheets_move_dimensions` | перенести строки или колонки в другое место листа |
| `gdocs_sheets_group_dimensions` | свернуть строки или колонки в группу |
| `gdocs_sheets_collapse_group` | сложить группу или раскрыть |
| `gdocs_sheets_sort_range` | отсортировать прямоугольник по колонкам |
| `gdocs_sheets_find_replace` | заменить текст в диапазоне, на листе или во всей книге |
| `gdocs_sheets_trim_whitespace` | убрать пробелы по краям ячеек |
| `gdocs_sheets_split_column` | разделить колонку по разделителю |
| `gdocs_sheets_auto_fill` | продолжить ряд, как протягивание за угол |
| `gdocs_sheets_add_chart` | построить столбчатую, линейную, площадную, точечную или круговую диаграмму |
| `gdocs_sheets_add_table` | превратить прямоугольник в таблицу с типами колонок |
| `gdocs_docs_create` | создать документ |
| `gdocs_docs_append` | дописать текст в конец |
| `gdocs_docs_insert_text` | вставить текст в позицию, в тело или в колонтитул |
| `gdocs_docs_replace_text` | заменить все вхождения строки |
| `gdocs_docs_style_text` | начертание, размер, шрифт, цвета и ссылка на диапазоне |
| `gdocs_docs_style_paragraph` | именованный стиль, выравнивание, отступы, интервалы, границы, заливка |
| `gdocs_docs_style_named` | что в этом документе значит NORMAL_TEXT, TITLE или заголовок |
| `gdocs_docs_style_document` | размер страницы, поля, фон, отступы колонтитулов |
| `gdocs_docs_make_bullets` | превратить абзацы в список, глубина — табуляцией |
| `gdocs_docs_insert_table` | вставить таблицу |
| `gdocs_docs_style_table` | заливки, границы, поля ячеек, ширины колонок, высоты строк, объединение, закреплённые строки |
| `gdocs_docs_insert_section_break` | начать раздел, который и несёт собственный колонтитул |
| `gdocs_docs_style_section` | поля и колонки одного раздела |
| `gdocs_docs_add_header_footer` | создать колонтитул и получить его сегмент |
| `gdocs_docs_insert_image` | картинка по адресу, до которого дотянется Google |
| `gdocs_docs_insert_page_break` | начать новую страницу |
| `gdocs_docs_insert_footnote` | сноска и сегмент, в который пишется её текст |
| `gdocs_docs_delete` | убрать что-то внутри документа: диапазон, строку или колонку таблицы, колонтитул, плавающий объект, маркеры списка |
| `gdocs_drive_copy` | скопировать файл |

## Доступ

Сервер работает от имени одного человека — того, кто прошёл вход. Сервис-аккаунта и
domain-wide delegation нет сознательно: ключ сервис-аккаунта с делегированием умеет
олицетворять любого сотрудника без его ведома, и образ с таким ключом — компрометация всей
компании, ждущая утечки. Настройка Google Workspace по полям и обоснования —
в [docs/SETUP.ru.md](SETUP.ru.md).

Два секрета, и они разделены:

- **OAuth-клиент** — это приложение. Либо файл, который скачивает Google Cloud Console,
  путь к нему в `--credentials`, либо две его половины в переменных
  `GOOGLE_OAUTH_CLIENT_ID` и `GOOGLE_OAUTH_CLIENT_SECRET` — именно это отдаёт хранилище
  паролей, и тогда о приложении на диске нет вообще ничего. Если заданы оба способа,
  побеждает окружение: оставшийся от прежней настройки файл не должен незаметно
  перевешивать то, что только что подсунули.
- **Токен** — это консент одного человека. Появляется при его собственном входе и остаётся
  файлом с правами 0600 в каталоге из `--token-dir`. Файлом, а не переменной, потому что
  сервер перезаписывает его при каждом обновлении access-токена, а хранилище секретов — не
  то место, куда процесс пишет сам.

Ни то, ни другое не коммитится и не собирается в образ: оба в `.gitignore` и
`.dockerignore`.

## Вход

На машине с браузером:

```bash
mcp-gdocs login --credentials ./gcp-oauth.keys.json --token-dir ./tokens
```

Команда печатает адрес, ждёт на loopback-порту возвращения браузера и пишет токен.

В контейнере браузера нет, поэтому вход идёт через страницу, которую сервер отдаёт сам.
Опубликуйте его порт на loopback и откройте:

```
http://127.0.0.1:8819/login?key=<MCP_AUTH_TOKEN этого сервера>
```

Страница говорит, входил ли кто-нибудь и что будет запрошено, а одна кнопка отправляет
браузер на экран согласия Google. Google возвращает его на `127.0.0.1` на тот же порт —
такой адрес гугловый клиент типа Desktop принимает без регистрации чего-либо в консоли, —
и по дороге назад пишется токен.

Ключ в адресе, потому что браузер не шлёт заголовок `Authorization` сам по себе; поэтому
страница отдаётся с `no-store` и `no-referrer`, а переход к Google — отправка формы, а не
ссылка: пересланный адрес `/login` покажет страницу, а не экран согласия.

Если сервер на другой машине — не выставляйте порт наружу, а пробросьте его: Google
возвращает только на loopback. `ssh -L 8819:127.0.0.1:8819 <хост>`.

## Запуск

```bash
mcp-gdocs --credentials /config/gcp-oauth.keys.json --token-dir /config/tokens --allow-write
```

| Флаг | Что делает |
|---|---|
| `--transport` | `stdio` (по умолчанию) или `streamable-http` |
| `--address` | адрес прослушивания для `streamable-http`, по умолчанию `127.0.0.1:8819` |
| `--credentials` | путь к файлу OAuth-клиента. Обязателен, если клиент не приходит из окружения |
| `--token-dir` | каталог, где живёт токен. Обязателен |
| `--scopes` | какие права запрашивать, через запятую. Пусто — значит `drive`, `spreadsheets`, `presentations`, `documents` |
| `--allow-write` | зарегистрировать инструменты, которые меняют документы. По умолчанию выключено |
| `--version` | напечатать версию и выйти |

Три переменные окружения — потому что секрет во флаге виден в списке процессов:

| Переменная | Что это |
|---|---|
| `MCP_AUTH_TOKEN` | bearer-токен, которого требует HTTP-транспорт. Без него сервер не стартует на `streamable-http`: всё, что дотянулось до этого порта, действует от имени вошедшего человека |
| `GOOGLE_OAUTH_CLIENT_ID` | OAuth-клиент вместо `--credentials`. Обе половины или ни одной |
| `GOOGLE_OAUTH_CLIENT_SECRET` | вторая половина |

Полный скоуп `drive`, а не `drive.file`, — сознательно: колода делается копированием
шаблона, а `drive.file` видит только файлы, созданные самим приложением, и потому шаблон
не откроет вовсе.

В контейнере — клиент подсовывается переменными, монтируется только токен:

```bash
docker run --rm -i --user "$(id -u):$(id -g)" \
  -e GOOGLE_OAUTH_CLIENT_ID -e GOOGLE_OAUTH_CLIENT_SECRET \
  -v "$HOME/.local/share/mcp-gdocs:/tokens" \
  ghcr.io/svesh87/mcp-gdocs:latest \
  --token-dir /tokens --allow-write
```

Образ собран `FROM scratch` и работает от 65532, поэтому тот, кто монтирует каталог
токенов, переопределяет пользователя своим идентификатором: иначе каталог не записываем и
сервер выходит, прямо об этом сказав.

## Навыки

По одному на редактор, потому что колода и книга портятся по-разному:

- [`skills/gdocs-slides/`](../skills/gdocs-slides/SKILL.md) — сборка колоды: порядок
  вызовов, что прочитать перед правкой и почему блоки никогда не двигают руками.
- [`skills/gdocs-sheets/`](../skills/gdocs-sheets/SKILL.md) — сборка книги: почему
  форматирование — это разложение на прямоугольники, а не заливка, и почему размер и
  локаль решаются при создании.

У каждого рядом два справочника — способ работы и факты об API стареют по-разному:
`references/controls.md` перечисляет все ручки по тому, что они меняют (чем читается, чем
пишется, в каких единицах), а `references/pitfalls.md` — ловушки, у каждой сказано, как она
выглядит в документе и чем лечится, а в конце то, чего API не сообщает вовсе.

## Разработка

```bash
gofmt -l .
go vet ./...
go test ./... -coverprofile=coverage.out
go tool cover -func=coverage.out | tail -1     # не ниже 80%
docker build -t mcp-gdocs:dev .
```

В тестах лежат **golden-файлы на тела запросов**: точный JSON, который сервер отправляет в
Slides, Sheets и Docs, — `internal/tools/testdata/`. В них весь смысл набора тестов, потому
что вид колоды решают именно эти тела, а не то, какие методы вызвались.
`go test ./... -update` их перезаписывает, и такая перезапись уместна в коммите, который
говорит, что изменилось в результате.

## Благодарности

Подход к правке презентаций — родные вложенные списки, стили с шаблона, настоящие таблицы,
миниатюры для проверки и никакого произвольного `batchUpdate` — взят с Node-сервера
коллеги, написанного под ту же задачу.

Формы запросов и работа с эндпойнтами сверялись с
[`piotr-agier/google-drive-mcp`](https://github.com/piotr-agier/google-drive-mcp) под
лицензией MIT. Код не копировался: здесь Go и своя реализация.
